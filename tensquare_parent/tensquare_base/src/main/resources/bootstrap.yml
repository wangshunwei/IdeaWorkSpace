spring:
  cloud:
    config:
      name: base
      profile: dev
      label: master
      discovery:
        enabled: true
        service‐id: tensquare‐config
#配置啦eureka的客户端
eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.25.130:6878/eureka/